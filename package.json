{
  "name": "bloom-atelier",
  "private": true,
  "scripts": {
    "dev": "npx nuxt dev --host 0.0.0.0 --port 3000",
    "build": "npx nuxt build",
    "preview": "npx nuxt preview",
    "postinstall": "npx nuxt prepare && node scripts/fix-bin-permissions.cjs"
  },
  "devDependencies": {
    "nuxt": "^3.11.0"
  },
  "dependencies": {
    "@nuxt/image": "^2.1.0"
  }
}
